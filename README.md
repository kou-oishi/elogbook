# Elogbook

A small electronic logbook for keeping chronological notes with Markdown and
file attachments. The application is intentionally simple: it behaves more like
a paper logbook than a threaded chat system.

## Project Layout

This repository is now a single Rust package. The source is still split by
responsibility, but the running application is one Actix server:

- `src/server/` - Actix, MongoDB access, attachment storage, API routes, and
  static file serving
- `src/frontend/` - Yew/WebAssembly UI code compiled by Trunk
- `src/api.rs` - shared API route constants, form field names, and limits
- `src/models.rs` - shared API DTOs used by both server and frontend
- `public/` - Trunk input assets: HTML, CSS, and browser JavaScript
- `dist/` - generated by `trunk build` and served by Actix; ignored by Git

Runtime data is deliberately kept out of Git. Local configuration lives in
`.env`, and uploaded files are stored under `attachments/`.

```mermaid
flowchart TB
    subgraph package["elogbook package"]
        direction TB
        main["<b>src/main.rs</b><br/>native server or wasm UI entry"]

        subgraph source["src/"]
            direction LR
            server["<b>server/</b><br/>Actix, MongoDB, attachments"]
            frontend["<b>frontend/</b><br/>Yew components + browser glue"]
            api["<b>api.rs</b><br/>paths, fields, limits"]
            models["<b>models.rs</b><br/>API DTOs"]
        end

        public["<b>public/</b><br/>HTML, CSS, JS assets"]
        trunk["<b>Trunk</b><br/>build WebAssembly bundle"]
        dist["<b>dist/</b><br/>generated static UI"]
    end

    main --> server
    main --> frontend
    server -. uses .-> api
    server -. uses .-> models
    frontend -. uses .-> api
    frontend -. uses .-> models
    public --> trunk
    frontend --> trunk
    api --> trunk
    models --> trunk
    trunk --> dist

    classDef entryStyle fill:#eef2ff,stroke:#4f46e5,color:#312e81;
    classDef serverStyle fill:#dcfce7,stroke:#16a34a,color:#14532d;
    classDef frontendStyle fill:#e0f2fe,stroke:#0284c7,color:#0c4a6e;
    classDef commonStyle fill:#f5f3ff,stroke:#7c3aed,color:#4c1d95;
    classDef assetStyle fill:#fff7ed,stroke:#ea580c,color:#7c2d12;

    class main entryStyle;
    class server serverStyle;
    class frontend frontendStyle;
    class api,models commonStyle;
    class public,trunk,dist assetStyle;
```

## Requirements

- Rust 1.88 or newer
- `wasm32-unknown-unknown` Rust target
- Trunk for building the Yew frontend
- MongoDB, either installed locally or run through Docker Compose

Useful setup commands:

```bash
rustup target add wasm32-unknown-unknown
cargo install trunk
```

## Module Map

The package has one Cargo manifest, with target-specific dependencies. Actix
dependencies are compiled for the native server target, while Yew/browser
dependencies are compiled for the WebAssembly target.

```mermaid
flowchart LR
    subgraph server["src/server"]
        direction TB
        smod["<b>mod.rs</b><br/>Actix app + static files"]
        sconfig["<b>config.rs</b><br/>env + filesystem paths"]
        shandlers["<b>handlers.rs</b><br/>/api routes"]
        sstate["<b>state.rs</b><br/>MongoDB + stores"]
        smodels["<b>models.rs</b><br/>DB/internal models"]
        sattach["<b>attachments.rs</b><br/>file uploads"]
        sdownload["<b>download.rs</b><br/>temporary tokens"]
        serror["<b>error.rs</b><br/>HTTP errors"]
    end

    subgraph common["src root common modules"]
        direction TB
        sapi["<b>api.rs</b><br/>paths, fields, limits"]
        sdto["<b>models.rs</b><br/>EntryResponse, AttachmentResponse"]
    end

    subgraph frontend["src/frontend"]
        direction TB
        fmod["<b>mod.rs</b><br/>Yew renderer"]
        fapp["<b>app.rs</b><br/>state + messages"]
        fapi["<b>api.rs</b><br/>same-origin HTTP client"]
        fmodels["<b>models.rs</b><br/>DTO to UI Entry"]
        fcomponents["<b>components.rs</b><br/>Yew views"]
        frender["<b>render.rs</b><br/>Markdown expansion"]
        fbridge["<b>js_bridge.rs</b><br/>EasyMDE callback"]
        fconfig["<b>config.rs</b><br/>API base + limits"]
    end

    smod --> sconfig
    smod --> shandlers
    smod --> sstate
    shandlers --> sstate
    shandlers --> smodels
    shandlers --> serror
    sstate --> sattach
    sstate --> sdownload
    sstate --> smodels
    sattach --> smodels
    sdownload --> smodels
    shandlers -.-> sapi
    shandlers -.-> sdto

    fmod --> fapp
    fapp --> fapi
    fapp --> fcomponents
    fapp --> fbridge
    fapp --> fconfig
    fapi --> fmodels
    fcomponents --> fmodels
    fcomponents --> frender
    frender --> fmodels
    fapi -.-> sapi
    fapi -.-> sdto
    fapp -.-> sapi
    fconfig -.-> sapi
    fmodels -.-> sdto

    classDef serverStyle fill:#dcfce7,stroke:#16a34a,color:#14532d;
    classDef commonStyle fill:#f5f3ff,stroke:#7c3aed,color:#4c1d95;
    classDef frontendStyle fill:#e0f2fe,stroke:#0284c7,color:#0c4a6e;

    class smod,sconfig,shandlers,sstate,smodels,sattach,sdownload,serror serverStyle;
    class sapi,sdto commonStyle;
    class fmod,fapp,fapi,fmodels,fcomponents,frender,fbridge,fconfig frontendStyle;
```

## Runtime Shape

Only one server is needed at runtime. Trunk builds the frontend into `dist/`;
Actix serves that static UI and the `/api/...` endpoints from the same origin.

```mermaid
flowchart LR
    user([User])
    browser["Browser<br/>http://127.0.0.1:8080"]

    subgraph runtime["single running server"]
        direction TB
        actix["<b>Actix server</b><br/>cargo run"]
        static["<b>static UI</b><br/>GET / from dist/"]
        api["<b>API</b><br/>/api/entries, /api/download"]
    end

    subgraph browser_runtime["browser runtime"]
        direction TB
        wasm["<b>Yew/WASM app</b><br/>loaded from dist/"]
        helper_js["<b>browser JS</b><br/>EasyMDE + previews"]
    end

    subgraph storage["local runtime data"]
        direction TB
        mongo[("MongoDB<br/>entries collection")]
        files[("attachments/<br/>uploaded files")]
        tokens["download token registry"]
    end

    user --> browser
    browser --> actix
    actix --> static
    static --> wasm
    static --> helper_js
    wasm -->|"same-origin HTTP"| api
    helper_js -->|"same-origin HTTP"| api
    actix --> api
    api --> mongo
    api --> files
    api --> tokens

    classDef actorStyle fill:#f8fafc,stroke:#64748b,color:#0f172a;
    classDef serverStyle fill:#dcfce7,stroke:#16a34a,color:#14532d;
    classDef frontendStyle fill:#e0f2fe,stroke:#0284c7,color:#0c4a6e;
    classDef dataStyle fill:#fef3c7,stroke:#d97706,color:#78350f;

    class user,browser actorStyle;
    class actix,static,api serverStyle;
    class wasm,helper_js frontendStyle;
    class mongo,files,tokens dataStyle;
```

## Configuration

Create a local environment file from the example:

```bash
cp .env.example .env
```

Then adjust the values as needed:

```dotenv
MONGODB_URI=mongodb://localhost:27017
DB_NAME=elogbook
SERVER_ADDR=127.0.0.1:8080
ATTACHMENTS_DIR=./attachments
WEB_DIR=./dist
```

The server stores new entries in the MongoDB `entries` collection. Attachments
are saved on disk and are served through short-lived download tokens.

## MongoDB Initialisation

For local development, the simplest option is Docker Compose:

```bash
docker compose up -d mongodb
```

This exposes MongoDB on `127.0.0.1:27017`, matching the default `.env.example`.

MongoDB creates the database and collection lazily, so no migration step is
required for a fresh local instance. On server start-up, the application also
ensures this index exists:

```javascript
db.entries.createIndex(
  { created_at: -1, _id: -1 },
  { name: "entries_by_created_at" }
)
```

If you prefer to initialise it manually:

```bash
mongosh mongodb://localhost:27017
```

```javascript
use elogbook
db.createCollection("entries")
db.entries.createIndex(
  { created_at: -1, _id: -1 },
  { name: "entries_by_created_at" }
)
```

For a local reset, discard the development database and uploaded files:

```bash
mongosh mongodb://localhost:27017 --eval 'db.getSiblingDB("elogbook").dropDatabase()'
rm -rf attachments/*
```

## Running Locally

Build the WebAssembly UI, then start the single Actix server:

```bash
trunk build
cargo run
```

Open:

```text
http://127.0.0.1:8080/
```

The server also exposes API endpoints for future external tools:

- `GET /api/health`
- `GET /api/entries?client=...&limit=...&offset=...`
- `POST /api/entries` with multipart fields `name`, `content`, and `file`
- `GET /api/download?client=...&token=...`
- `POST /api/download/extend?client=...`

## Data Flow

Adding an entry:

```mermaid
sequenceDiagram
    autonumber
    actor User
    participant Editor as EasyMDE / textarea.js
    participant App as Yew app
    participant API as Actix /api
    participant Files as attachments dir
    participant DB as MongoDB

    User->>Editor: Write Markdown and attach files
    Editor->>App: send_add_entry(content, files)
    App->>API: POST /api/entries (multipart/form-data)
    API->>Files: Save each uploaded file
    API->>DB: Insert Entry document
    API-->>App: 201 Created
    App->>API: GET /api/entries?limit=1
    API-->>App: Latest EntryResponse
    App-->>User: Append rendered entry
```

Loading and previewing entries:

```mermaid
sequenceDiagram
    autonumber
    participant App as Yew app
    participant API as Actix /api
    participant DB as MongoDB
    participant Preview as preview.js
    participant Files as attachments dir

    App->>API: GET /api/entries?client=...&limit=...
    API->>DB: Query entries sorted by created_at
    API->>API: Register temporary download tokens
    API-->>App: EntryResponse[] with attachment tokens
    App-->>Preview: Render attachment placeholders
    Preview->>API: GET /api/download?client=...&token=...
    API->>Files: Open saved attachment
    API-->>Preview: File bytes
    Preview-->>App: Replace placeholder with preview
```

## Development Checks

Run the native server checks from the repository root:

```bash
cargo fmt --all --check
cargo check
cargo test
cargo clippy --all-targets -- -D warnings
```

Check and build the WebAssembly UI:

```bash
cargo check --target wasm32-unknown-unknown
cargo clippy --target wasm32-unknown-unknown -- -D warnings
trunk build
```

If your shell exports `NO_COLOR=1` and Trunk rejects it, run:

```bash
env -u NO_COLOR trunk build
```
