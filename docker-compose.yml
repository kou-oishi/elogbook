services:
  mongodb:
    image: mongo:8
    container_name: elogbook-mongodb
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - mongodb-data:/data/db

volumes:
  mongodb-data:
